services:

  frontend:
    build:
      context: ./frontend
    container_name: myapp_frontend
    restart: unless-stopped
    ports:
      - "3001:80"

  backend:
    build:
      context: ./backend
    container_name: myapp_backend
    restart: unless-stopped
    ports:
      - "4001:3000"